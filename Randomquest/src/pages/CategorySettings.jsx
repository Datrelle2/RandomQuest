import CategorySelection from './CategorySelection'

// Reuses the same component — the isNew flag drives the different UX path
export default function CategorySettings() {
  return <CategorySelection />
}
